이 프로젝트는 CLI 기반 Todo 관리 앱입니다.

- 모든 비즈니스 로직은 사용자 인터페이스와 분리된 독립 레이어에서 작동해야 합니다.
- 테스트 코드를 먼저 작성하고, 테스트 없는 구현 코드는 사용하지 않습니다.
- 외부 패키지 설치 전에 반드시 필요성을 검토하며, 불필요한 의존성을 만들지 않습니다.
- 지금 당장 필요하지 않은 추상화 레이어는 만들지 않고, 명확하고 직접적인 구현을 선호합니다.
- 이 프로젝트는 터미널 CLI 도구를 만드는 것을 목표로 하며, REST API 서버, GUI, 웹 인터페이스는 범위 밖입니다.
